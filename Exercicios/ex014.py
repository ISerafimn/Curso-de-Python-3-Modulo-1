# Escreva um programa que converta uma temperatura digitada em C° e converta para F°.

c = float(input('Digite a Temperatura em C° atual: '))

print('A temperatura em F° é: {:.1f}°'.format(c * 1.8 + 32))

print('A temperatura em F° é: {:.1f}°'.format(9 * c / 5 + 32))
